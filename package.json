{
  "name": "memact-schema",
  "private": true,
  "version": "0.0.0",
  "description": "Evidence-induced virtual cognitive schema layer for Memact.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./src/engine.mjs",
  "exports": {
    ".": "./src/engine.mjs",
    "./cli": "./src/cli.mjs"
  },
  "keywords": [
    "memact",
    "schema",
    "cognitive-schema",
    "deterministic",
    "evidence-induced",
    "memory"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Memact/Schema.git"
  },
  "homepage": "https://github.com/Memact/Schema",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "memact-schema": "./src/cli.mjs"
  },
  "scripts": {
    "check": "node ./scripts/check.mjs",
    "schema": "node ./src/cli.mjs",
    "sample": "node ./src/cli.mjs --input examples/sample-inference-output.json --format report",
    "sample:json": "node ./src/cli.mjs --input examples/sample-inference-output.json --format json"
  }
}
