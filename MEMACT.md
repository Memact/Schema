# Memact Schema Notes

Schema organizes semantic evidence into cognitive-style packets.

Packets can have categories and sub-schema direction. For example,
discount-related signals can sit inside a shopping schema.

Schema does not capture activity, infer raw meaning, store memory, or run
features.
